[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"


[project]
name = "psy-experiments"
version = "0.1.0"
description = "Psychological experiments written in PsychoPy"
requires-python = ">=3.11"


[tool.setuptools.packages.find]
where = ["."]